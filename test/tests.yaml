- suite: 'Basic Sanity Tests'
    requests:
    - command: PING
      head-only: true
      compression: none
      response-compression: none
      # TODO:
      # headers:
      # body:
      expected-marshal: "EWP 0.1 PING none none 0 0 H"
    - command: PING
      head-only: false
      compression: none
      response-compression: none
      # TODO:
      # headers:
      # body:
      expected-marshal: "EWP 0.1 PING none none 0 0"
    responses:
    - status: 200
      head-only: true
      compression: none
      # TODO:
      # headers:
      # body:
      expected-marshal: "200 none 0"
    - status: 200
      head-only: false 
      compression: none
      # TODO:
      # headers:
      # body:
      expected-marshal: "200 none 0 0"
